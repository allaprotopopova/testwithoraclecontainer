spring:
  application:
    name: TestContainersSampleApplication
  datasource:
    platform: oracle
    name: oracle
    driver-class-name:  oracle.jdbc.OracleDriver
    username: PERSONAL
    password: passw0rd
  jpa:
    database: ORACLE
    generate-ddl: true
    hibernate.ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        use_sql_comments: true
        connection.charSet: UTF-8
        format_sql: true
        cache:
          use_second_level_cache: false
          use_query_cache: false

evilcorp:
  testcontainers:
    enabled: true


